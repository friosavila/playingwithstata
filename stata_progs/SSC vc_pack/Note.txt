To do:
Add weights to vc_predict vc_reg and vc_preg
I think i only need to do it with predict. need to figure out right way to count observations
\sum(w_i/E(w_i))

Modify vc_bw so everything is done within mata, or so we change from st_data to st_view
This should be easier to load data, 